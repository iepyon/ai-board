# GitLab の root パスワード。compose が読む。
# .env にコピーして実際の値を入れる（.env は追跡しない）。
# GitLab の要求により 8 文字以上。
GITLAB_ROOT_PASSWORD=

# ai-board 本体はこのファイルを読まない。
# レビュー要求の取得先への認証は gh / glab のログイン状態に委ねており、
# アクセストークンを設定する必要は無い。
