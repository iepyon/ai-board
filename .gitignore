node_modules/
dist/
coverage/
*.log
.DS_Store

# ループが書く実行状態。共有しない
.ai-board/loop.json

# GitLab の root パスワード。compose が読む。共有しない
.env

# アップグレード前に取り出す GitLab のバックアップと secrets。共有しない
/gitlab-backup/

# 各自の GitLab 接続設定。config.example.yaml をコピーして使う
/.ai-board/config.yaml
