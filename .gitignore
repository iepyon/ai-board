node_modules/
dist/
coverage/
*.log
.DS_Store

# カードの正本（SQLite）。各自のローカルに置き、共有しない
/.ai-board/board.db
/.ai-board/board.db-*

# ループが書く実行状態。共有しない
.ai-board/loop.json

# 廃止した GitLab 連携の名残（.env / バックアップと secrets）。手元に残っていても共有しない
.env
/gitlab-backup/

# 各自の GitHub 接続設定。config.example.yaml をコピーして使う
/.ai-board/config.yaml
